---
title: Transaktioiden tarkastelu
description:
  Dekoodaa ja tarkastele vahvistettua Solana-transaktiota
  @solana/transaction-introspection-paketin avulla – ratkaise tilitunnisteet ja
  hakutaulukon osoitteet, käy läpi ulkoiset ja sisäiset (CPI) ohjeet sekä
  jäsennä ne ohjelmaklienttien avulla.
url: /docs/core/transactions/transaction-introspection
type: conceptual
prerequisites:
  - /docs/core/transactions/transaction-structure
  - /docs/core/instructions/instruction-structure
related:
  - /docs/core/transactions/versioned-transactions
  - /docs/rpc/http/gettransaction
---

<Callout type="info" title="Yhteenveto">
  `@solana/transaction-introspection` muuntaa `getTransaction` RPC-vastauksen
  ohjeluetteloksi, jossa on todelliset osoitteet. Se ratkaisee tilitunnisteet
  (mukaan lukien hakutaulukon osoitteet), dekoodaa sisäiset CPI-ohjeet ja käy
  läpi koko ohjerakenteen lohkoselailijan järjestyksessä.
</Callout>

<Callout type="info">
  Etsitkö tietoa siitä, miten ohjelma tarkastaa sukulaisohjeet _ketjussa_
  ajonaikaisesti? Se on Instructions sysvar – katso [Ohjeiden
  tarkastelu](/docs/core/instructions/instruction-introspection). Tämä sivu
  käsittelee transaktion dekoodaamista _ketjun ulkopuolella_ RPC-vastauksesta.
</Callout>

[`getTransaction`](/docs/rpc/http/gettransaction)-vastaus ei tarjoa sinulle
valmista ohjeluetteloa. Tilit tallennetaan numeerisina tunnisteinä,
[versioitujen transaktioiden](/docs/core/transactions/versioned-transactions)
tilit jaetaan staattisiin avaimiin sekä
[osoitehakutaulukoista](/docs/core/transactions/versioned-transactions)
ladattuihin osoitteisiin, ja Cross Program Invocation (CPI) -sisäiset ohjeet
saapuvat koodattuina rakenteina transaktion metatiedoissa.

`@solana/transaction-introspection` tekee tämän selvityksen puolestasi. Se
dekoodaa lanka-transaktion, yhdistää jokaisen tilitunnisteen vastaavaan
`Address`-arvoon, normalisoi sisäiset ohjeet ja palauttaa ohjeet samassa
ulkoisesta sisäiseen -järjestyksessä kuin lohkoselailija näyttää. Palautetut
ohjeet sopivat suoraan codama-generoitujen klienttien (esim.
`@solana-program/*`) `parseXInstruction` -apufunktioihin.

```shell title="Terminal"
npm install @solana/transaction-introspection @solana/kit @solana/instructions @solana-program/token
```

## Dekoodaa vastaus

Hae transaktio lanka-muotoisella koodauksella ja välitä vastaus
`decodeTransactionFromRpcResponse`-funktiolle.

```ts title="Decode a transaction"
import { createSolanaRpc, signature } from "@solana/kit";
import { decodeTransactionFromRpcResponse } from "@solana/transaction-introspection";

const rpc = createSolanaRpc("https://api.mainnet-beta.solana.com");

const txid =
  "3jUKrQp1UGq5ih6FTDUUt2kkqUfoG2o4kY5T1DoVHK2tXXDLdxJSXzuJGY4JPoRivgbi45U2bc7LZfMa6C4R3szX";

const rpcTx = await rpc
  .getTransaction(signature(txid), {
    commitment: "confirmed",
    encoding: "base64",
    maxSupportedTransactionVersion: 0
  })
  .send();
if (!rpcTx) throw new Error(`Transaction ${txid} not found`);

const { compiledMessage, loadedAddresses } =
  decodeTransactionFromRpcResponse(rpcTx);
```

Jäljellä olevat koodinäytteet käyttävät uudelleen tämän vaiheen `rpcTx`-,
`compiledMessage`- ja `loadedAddresses`-muuttujia.

<Callout type="warn">
  Hae käyttämällä `base64`-, `base58`- tai `json`-koodausta.
  `decodeTransactionFromRpcResponse` hylkää `jsonParsed`-vastaukset – kyseinen
  koodaus pyytää RPC-solmua jäsentämään ohjeet palvelinpuolella, joten
  asiakaspuolella ei ole enää mitään dekoodattavaa.
</Callout>

## Käy läpi instruktiot

`walkInstructions` palauttaa jokaisen instruktion – ulommat ja sisemmät –
taulukkona siinä järjestyksessä kuin lohkoselain näyttää: kukin ulompi
instruktio välittömästi sitä seuranneiden CPI-kutsujen tuottamien sisempien
instruktioiden kanssa. Jokaisessa instruktiossa on `trace`- kenttä, joka kuvaa
sen sijaintia kutsujen hierarkiassa.

```ts title="Walk every instruction"
import { walkInstructions } from "@solana/transaction-introspection";

for (const ix of walkInstructions({
  compiledMessage,
  loadedAddresses,
  meta: rpcTx.meta
})) {
  const location =
    ix.trace.kind === "outer"
      ? `outer[${ix.trace.index}]`
      : `inner[${ix.trace.outerIndex}/${ix.trace.innerIndex}]`;
  console.log(location, ix.programAddress, ix.accounts?.length ?? 0);
}
```

`trace` on eritelty unioni: `{ kind: "outer", index }` ylimmän tason
instrutiolle tai `{ kind: "inner", outerIndex, innerIndex, stackHeight? }`
ulomman instruktion alle sisäkkäiselle CPI-instruktiolle.

`meta` on valinnainen – välitä `rpcTx.meta` suoraan, vaikka se saattaisi olla
`null`. Ilman metadataa `walkInstructions` palauttaa vain ulommat instruktiot.

## Jäsennä ohjelmaklientillä

Jokainen `walkInstructions`:n palauttama instruktio on `ResolvedInstruction`,
joten se toimii suoraan `@solana/instructions`:n predikaattien sekä
`@solana-program/*`-klienttien tuottamien `identifyXInstruction`- /
`parseXInstruction`-apurien kanssa – ilman muunnosvaihetta.

Tämä esimerkki tarkastaa jokaisen Token Program `SyncNative` -instruktion
transaktiossa, riippumatta siitä, suoritettiinko se ylimmällä tasolla vai CPI:n
sisällä.

```ts title="Find and parse SyncNative instructions"
import {
  isInstructionForProgram,
  isInstructionWithAccounts,
  isInstructionWithData
} from "@solana/instructions";
import { walkInstructions } from "@solana/transaction-introspection";
import {
  identifyTokenInstruction,
  parseSyncNativeInstruction,
  TOKEN_PROGRAM_ADDRESS,
  TokenInstruction
} from "@solana-program/token";

for (const ix of walkInstructions({
  compiledMessage,
  loadedAddresses,
  meta: rpcTx.meta
})) {
  if (!isInstructionForProgram(ix, TOKEN_PROGRAM_ADDRESS)) continue;
  if (!isInstructionWithData(ix) || !isInstructionWithAccounts(ix)) continue;
  if (identifyTokenInstruction(ix) !== TokenInstruction.SyncNative) continue;

  const parsed = parseSyncNativeInstruction(ix);
  console.log(ix.trace, parsed);
}
```

## Matalamman tason apurit

`walkInstructions` kattaa useimmat tarpeet, mutta paketti paljastaa myös sen
rakennuspalikat:

- `getInstructionsFromCompiledTransactionMessage(compiledMessage, loadedAddresses?)`
  palauttaa vain ulommat instruktiot selvitettyine tilineen ja purettuine
  tietoineen.
- `getInnerInstructionsFromMeta(meta, accountMetas)` purkaa sisemmät CPI-
  instruktiot transaktion metadatasta.
- `getAccountMetasFromCompiledTransactionMessage(compiledMessage, loadedAddresses?)`
  palauttaa koko tililuettelon transaktiojärjestyksessä – se syöte, jonka
  edellinen apuri tarvitsee sisempien instruktioiden tili-indeksien
  selvittämiseen.

```ts title="Resolve outer and inner instructions separately"
import {
  getAccountMetasFromCompiledTransactionMessage,
  getInnerInstructionsFromMeta,
  getInstructionsFromCompiledTransactionMessage
} from "@solana/transaction-introspection";

const outer = getInstructionsFromCompiledTransactionMessage(
  compiledMessage,
  loadedAddresses
);

const accountMetas = getAccountMetasFromCompiledTransactionMessage(
  compiledMessage,
  loadedAddresses
);

if (!rpcTx.meta) throw new Error("Transaction metadata missing");
const inner = getInnerInstructionsFromMeta(rpcTx.meta, accountMetas);
```

<Callout type="info" title="Käytätkö Rustia?">
  Tälle paketille ei ole Rust-vastinetta. Saadaksesi jäsennettyjä instruktioita
  Rustissa, pyydä `UiTransactionEncoding::JsonParsed`
  [`getTransaction`](/docs/rpc/http/gettransaction) -kutsulla ja anna RPC-solmun
  selvittää tilit ja sisemmät instruktiot puolestasi.
</Callout>

## Katso myös

- [Tapahtuman rakenne](/docs/core/transactions/transaction-structure) - tämän
  paketin dekoodaaman vastauksen anatomia
- [Versioidut tapahtumat](/docs/core/transactions/versioned-transactions) -
  kuinka Address Lookup Tables jakaa tilit staattisiin ja ladattuihin ryhmiin
- [`getTransaction`](/docs/rpc/http/gettransaction) - RPC-metodi, joka palauttaa
  tapahtuman
